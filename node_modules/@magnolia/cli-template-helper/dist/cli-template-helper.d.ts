import { PluginTemplate } from '@magnolia/cli-plugin-template';
import { Option } from 'commander';
import { Logger } from 'winston';
import { Context, TemplateHelperArgs, PluginArgs, PluginOptions } from './types/types.js';
export declare let logger: Logger;
export declare let i18nInstance: {
    t(key: string, options?: any): string;
};
export default abstract class TemplateHelperPlugin extends PluginTemplate {
    pluginArgs: PluginArgs;
    options: Option[];
    context: Context;
    tempFolder: string;
    configUpdates: any;
    usingMessages: any;
    constructor(args: PluginArgs, context: Context);
    init(winstonLogger: Logger): Promise<void>;
    start(options: PluginOptions, args?: string[]): Promise<void>;
    stop(): Promise<void>;
    private validateAndResolveArgs;
    private getFw;
    private downloadFramework;
    protected startFramework(args: TemplateHelperArgs, options: PluginOptions): Promise<void>;
}
