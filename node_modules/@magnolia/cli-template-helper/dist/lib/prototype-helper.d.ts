import { IPreparedComponentItem, IPrototypeHelperOptions, ITemplateArgs, ITemplateData } from '../types/types.js';
export declare class PrototypeHelper {
    options: IPrototypeHelperOptions;
    templateData: ITemplateData;
    templateArgs: ITemplateArgs;
    preparedComponent: IPreparedComponentItem[];
    configUpdates: any;
    usingMessages: any;
    constructor(name: string, options: IPrototypeHelperOptions, usingMessages?: any);
    start(): Promise<any>;
    stop(message: string | undefined): Promise<void>;
    loadPrototypeConfigFile(pathToPrototype: string): Promise<void>;
    loadTemplateDataAndArgsFromOptions(): Promise<void>;
    getPathToPrototype(): Promise<string>;
    getPathToPrototypeLM(pathToPrototype: string): Promise<string>;
    private getCorrectConfigValue;
    validateHeadlessOptions(): Promise<void>;
    getPathToPrototypeSPA(pathToPrototype: string): Promise<string>;
    getProjectPJ(): Promise<any>;
    prepareComponentFromPrototype(prototypeTemplatePath: string, componentDestinationPath: string): Promise<void>;
    preventDuplicates(): Promise<void>;
    create(): Promise<void>;
    buildMappingObject(): Promise<{
        id: string;
        name: string;
        postfix: string;
    }>;
    buildImportObject(mappingName: string, mappingPostfix: string): Promise<{
        name: string;
        source: string;
    }>;
    getImportString(name: string, source: string): Promise<string>;
    getMappingString(id: string, name: string): Promise<string>;
    promptForPotentialDuplicity(item: {
        first: string | undefined;
        second: string | undefined;
    }, condition: boolean, stringBuilder: (arg1: string, arg2: string) => Promise<string>, expectedString: string, msgConfig: {
        type: 'import' | 'mapping';
        first: string;
        second: string;
    }): Promise<string>;
    getAndValidateImportString(componentMappingFileContent: string, importObj: {
        name: string;
        source: string;
    }): Promise<string>;
    getAndValidateMappingString(match: RegExpMatchArray | null, mappingObj: {
        id: string;
        name: string;
        postfix: string;
    }): Promise<string>;
    promptUser(msg: string): Promise<boolean>;
    writeComponentMapping(importObj: {
        name: string;
        source: string;
    }, mappingObj: {
        id: string;
        name: string;
        postfix: string;
    }): Promise<void>;
}
