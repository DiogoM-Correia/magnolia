import { PluginTemplate } from '@magnolia/cli-plugin-template';
import { Option } from 'commander';
import { Logger } from 'winston';
import { PluginArgs, PluginOptions, Type } from './types/types.js';
export declare const cliPrototypesPath: string;
export declare let logger: Logger;
export declare let i18nInstance: {
    t(key: string, options?: any): string;
};
export default abstract class ContentHelperPlugin extends PluginTemplate {
    pluginArgs: PluginArgs;
    options: Option[];
    context: Type;
    configUpdates: any;
    constructor(args: PluginArgs, context: Type);
    init(winstonLogger: Logger): Promise<void>;
    start(options: PluginOptions): Promise<void>;
    stop(): Promise<void>;
    private validateAndResolveArgs;
    private create;
    private createEntity;
    private handleAppsTranslation;
    private handlePrompt;
    private namePrompt;
    private checkFileExistence;
    private getPluginName;
}
