import { PluginTemplate } from '@magnolia/cli-plugin-template';
import { Option } from 'commander';
import { PluginOptions } from './types/types.js';
import { Logger } from 'winston';
import { i18n } from 'i18next';
export declare let logger: Logger;
export declare let i18nInstance: i18n;
export default class GeneratePlugin extends PluginTemplate {
    name: string;
    version: any;
    usage: string;
    description: string;
    options: Option[];
    constructor();
    start(options: PluginOptions): Promise<void>;
    stop(): Promise<void>;
    init(winstonLogger: Logger): Promise<void>;
}
