import { ChildProcess } from 'child_process';
export interface PluginOptions {
    tomcat?: string | boolean;
    lightModulesPath?: string | boolean;
}
export interface MagnoliaProject {
    tomcatPath: string;
    lightModulesPath?: string;
    childProcess?: ChildProcess;
    start: () => Promise<void>;
    stop: () => Promise<void>;
}
export interface PluginArgs {
    tomcatPath?: string;
}
export interface ConfigArgs {
    lightModulesPath?: string;
}
