import { Option } from 'commander';
import { Logger } from 'winston';
import { PluginTemplate } from '@magnolia/cli-plugin-template';
import { PluginArgs, PluginOptions } from './types/types.js';
export declare let logger: Logger;
export declare let i18nInstance: {
    t(key: string, options?: any): string;
};
export default class CreateLightModulePlugin extends PluginTemplate {
    pluginName: string;
    name: string;
    version: any;
    description: string;
    usage: string;
    options: Option[];
    args: PluginArgs;
    configUpdates: any;
    constructor(args: PluginArgs);
    init(winstonLogger: Logger): Promise<void>;
    start(options: PluginOptions): Promise<void>;
    stop(): Promise<void>;
    private validateAndResolveArgs;
    private createModule;
    private createModuleDescriptor;
}
