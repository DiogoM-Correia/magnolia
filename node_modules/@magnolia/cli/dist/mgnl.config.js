import GeneratePlugin from '@magnolia/cli-generate-plugin';
import JumpstartPlugin from '@magnolia/cli-jumpstart-plugin';

export default {
	plugins: [new GeneratePlugin(), new JumpstartPlugin()],
	tabCompletion: true,
	logger: {
		filename: './mgnl.error.log',
		fileLevel: 'debug',
		consoleLevel: 'info',
	},
};
