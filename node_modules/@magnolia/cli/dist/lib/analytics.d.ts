import { PluginTemplate } from '@magnolia/cli-plugin-template';
declare function send(cliVersion: string, config: any, plugin: PluginTemplate): Promise<void>;
declare function init(configPath: string): Promise<void>;
export declare const analytics: {
    send: typeof send;
    init: typeof init;
};
export {};
