import { PluginTemplate } from '@magnolia/cli-plugin-template';
import { Logger } from 'winston';
export declare let logger: Logger;
export declare let i18nInstance: {
    t(key: string, options?: any): string;
};
export default class Init extends PluginTemplate {
    name: string;
    usage: string;
    description: string;
    version: string;
    configPath: string;
    pjPath: string;
    private projectType?;
    private configurationSummary;
    private deferredErrors;
    constructor(args: any);
    start(): Promise<void>;
    stop(): Promise<void>;
    init(winstonLogger: Logger): void;
    private isProjectAlreadyInitialized;
    private detectProjectType;
    private hasApacheTomcatStructure;
    private isHeadlessAcceleratorProject;
    private initProject;
    private ensurePackageJsonExists;
    private ensureMagnoliaCliInstalled;
    private ensureMgnlScriptExists;
    private ensureConfigFileIsValid;
    private extractPackageNamesFromConfig;
    private ensureConfigPackagesInstalled;
    private handleHAProjectSetup;
    private showSummary;
}
