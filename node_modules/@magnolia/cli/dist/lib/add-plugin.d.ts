import { PluginTemplate } from '@magnolia/cli-plugin-template';
import { Logger } from 'winston';
export default class AddPlugin extends PluginTemplate {
    name: string;
    usage: string;
    description: string;
    version: string;
    loggerConfig: string;
    logger?: Logger;
    configPath: string;
    pjPath: string;
    i18nInstance: any;
    constructor(args: any);
    start(): Promise<void>;
    stop(): Promise<void>;
    init(logger: Logger): void;
    private validateArgs;
    private addPlugin;
    protected execaWrapper(name: string, options: string[]): Promise<import("execa").Result<{}>>;
    private installPluginFromSource;
    private parseClassNameFromPackage;
    private isPluginAlreadyInstalled;
    private isClassNameAvailable;
    private appendNumToClassName;
    private modifyConfig;
    private getPJDependencies;
    private getPackageManager;
    private buildImportString;
    private parseScopeAndName;
    private stripVersion;
}
