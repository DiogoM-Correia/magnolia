import { PluginTemplate } from '@magnolia/cli-plugin-template';
import { Logger } from 'winston';
export interface PluginOptions {
    disable: boolean;
    uninstall: boolean;
    reinstall: boolean;
    resetCache: boolean;
}
export default class TabCompletion extends PluginTemplate {
    name: string;
    usage: string;
    i18nInstance: any;
    description: string;
    version: string;
    logger?: Logger;
    paths: {
        MGNL_V5_DIR_PATH: string;
        TAB_COMP_SCRIPT_PATH: string;
        WINDOWS_USER_PROFILE_PATHS: string[];
        USER_SHELL_CONFIG_PATHS: string[];
    };
    markers: {
        SHELL_AND_PROFILE_START: string;
        SHELL_AND_PROFILE_END: string;
        TAB_COMPLETION_START: string;
        TAB_COMPLETION_END: string;
    };
    systemScriptsPaths: string[];
    constructor(args: any);
    start(options: PluginOptions): Promise<void>;
    stop(): Promise<void>;
    init(logger: Logger): void;
    private toggle;
    private install;
    private isV4Configured;
    private checkDependencies;
    private ensureTabCompletionScript;
    private getTabCompletionLoaderScript;
    private configureTabCompletionLoader;
    private containsMarkers;
    private stripMarkersAndContent;
    private logResult;
    private canWriteToConfig;
    private addConfig;
    private resetCache;
    private uninstall;
}
